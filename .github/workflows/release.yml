name: goreleaser

on:
  push:
    # Pattern matched against refs/tags
    tags:        
      - '**'           # Push events to every tag including hierarchical tags like v1.0/beta

permissions:
  contents: write
  # packages: write
  # issues: write
  # id-token: write

jobs:
  goreleaser:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: stable
      - name: Install Zig for CGO cross-compilation
        run: |
          wget -q https://ziglang.org/download/0.15.2/zig-linux-x86_64-0.15.2.tar.xz
          tar xf zig-linux-x86_64-0.15.2.tar.xz
          sudo mv zig-linux-x86_64-0.15.2 /usr/local/zig
          echo "/usr/local/zig" >> $GITHUB_PATH
      - name: Verify Zig installation
        run: zig version
      - name: Set Zig version env var
        run: echo "ZIG_VERSION=$(zig version)" >> $GITHUB_ENV
      - name: Check if release already exists
        id: release-check
        run: |
          if gh release view "${GITHUB_REF_NAME}" >/dev/null 2>&1; then
            echo "exists=true" >> $GITHUB_OUTPUT
            echo "⚠️  Release for tag ${GITHUB_REF_NAME} already exists"
          else
            echo "exists=false" >> $GITHUB_OUTPUT
            echo "✅ No existing release found"
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Delete existing release if present
        if: steps.release-check.outputs.exists == 'true'
        run: |
          echo "Deleting existing release ${GITHUB_REF_NAME}..."
          gh release delete "${GITHUB_REF_NAME}" --yes --cleanup-tag=false
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Setup syft
        uses: anchore/sbom-action/download-syft@latest # installs syft
      # More assembly might be required: Docker logins, GPG, etc.
      # It all depends on your needs.
      - name: Run GoReleaser with Zig
        uses: goreleaser/goreleaser-action@v7
        with:
          # either 'goreleaser' (default) or 'goreleaser-pro'
          distribution: goreleaser
          # 'latest', 'nightly', or a semver
          version: "~> v2"
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}